import Vue from 'vue'
import Vuex from 'vuex'
import studentList from './modules/studentList'

Vue.use(Vuex)

export default new Vuex.Store({
  state,
  mutations: {
  },
  actions: {
  },
  modules: {
    studentList
  }
})
